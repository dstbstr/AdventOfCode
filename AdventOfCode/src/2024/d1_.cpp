#include "Common.h"

SOLUTION(2024, 1) {
    PART(1) {
        return Constexpr::ToString(lines.size());
    }

    PART(2) {
		return Constexpr::ToString(lines.size());
    }

    TEST(1) {
        return true;
    }
}